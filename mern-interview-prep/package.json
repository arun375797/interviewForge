{
  "name": "mern-interview-prep",
  "version": "1.0.0",
  "private": true,
  "description": "InterviewForge — MERN interview prep for JS, React, Node.js, DSA",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend --include=dev",
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix backend --omit=dev",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend"
  }
}
